module joc-eda-backend

go 1.22

// Phase 14 (Option A) — Sentry SDK for distributed tracing + breadcrumbs.
// Run `go mod tidy` after `go.sum` checksums become available locally.
require github.com/getsentry/sentry-go v0.30.0

require (
	golang.org/x/sys v0.18.0 // indirect
	golang.org/x/text v0.14.0 // indirect
)
